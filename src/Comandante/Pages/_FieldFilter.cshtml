@using System;
@using System.Linq;
@using  Comandante;
@using  Comandante.Services;

@if (Model.ClrType == typeof(string) || Model.ClrType.IsNumericType())
{
    <input name="@Model.Name" value="@Value" />
}

@if (Model.ClrType == typeof(bool) || Model.ClrType == typeof(bool?))
{
    <select name="@Model.Name">
        <option value="">Not specofied</option>
        <option value="true">True</option>
        <option value="false">False</option>
    </select>
}

@if (Model.ClrType == typeof(DateTime) || Model.ClrType == typeof(DateTime?) || Model.ClrType == typeof(DateTimeOffset) || Model.ClrType == typeof(DateTimeOffset?))
{
    <input name="@Model.Name" value="@Value" type="datetime-local" />
}