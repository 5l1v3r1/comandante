@using System;
@using System.Linq;
@using  Comandante;
@using  Comandante.Services;

@if (Model.Field.ClrType == typeof(string) || Model.Field.ClrType.IsNumericType())
{
    <textarea name="@Model.Field.Name" style="min-width: 300px" rows="1">@Model.Value</textarea>
}
else
@if (Model.Field.ClrType == typeof(bool))
{
    <input name="@Model.Field.Name" value="True" type="checkbox" @(@Model.Value == "True" ? "checked" : "")/>
}
else
if (Model.Field.ClrType == typeof(bool?))
{
    <select name="@Model.Field.Name"  style="min-width: 300px">
        <option value="">Not specofied</option>
        <option value="True" @(@Model.Value == "True" ? "selected" : "")>True</option>
        <option value="False" @(@Model.Value == "False" ? "selected" : "")>False</option>
    </select>
}
else
if (Model.Field.ClrType == typeof(DateTime) || Model.Field.ClrType == typeof(DateTime?) || Model.Field.ClrType == typeof(DateTimeOffset) || Model.Field.ClrType == typeof(DateTimeOffset?))
{
    <input name="@Model.Field.Name" value="@Model.Value" type="datetime-local"  style="min-width: 300px" />
}
else
{
    <div class="small text-muted">field type is not supported</div>
}